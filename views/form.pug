form(action="/api/convert", method="get" id="convertForm")
    input(id="input_query" type="text", name="input" placeholder="km, mi, kg, lbs, l, gal")
    button(type="submit") Convert
    p#api_result Result